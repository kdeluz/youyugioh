<% content_for :title, "Contact Us" %>

<header class="page-head">
  <p class="eyebrow">Get in touch</p>
  <h1 class="page-title">Contact <em>us</em></h1>
</header>

<div class="prose">
  <% if @contact_page %>
    <%= @contact_page.content.html_safe %>
  <% else %>
    <p>Questions about an order or a card's condition? We'd love to hear from you.</p>
  <% end %>
</div>
