<% content_for :title, "Admin" %>

<header class="page-head">
  <p class="eyebrow">Staff only</p>
  <h1 class="page-title">Admin <em>dashboard</em></h1>
  <p class="lede">Welcome back, <%= current_user.username %>. Manage the catalogue, orders and site content from here.</p>
</header>

<div class="actions-row">
  <%= link_to "Manage orders", admin_orders_path, class: "btn btn-primary" %>
  <%= link_to "ActiveAdmin", admin_root_path, class: "btn btn-ghost" %>
  <%= link_to "Back to shop", root_path, class: "btn btn-quiet" %>
</div>
